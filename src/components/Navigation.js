import React from 'react';
import { Container, Navbar, Nav} from 'react-bootstrap';

function Navigation(props) {

  return (
    <Navbar>
      {/* IF LOGGED IN SHOW LOGIN, ELSE SHOW LOGOUT */}
    </Navbar>
  );
}

export default Navigation;
